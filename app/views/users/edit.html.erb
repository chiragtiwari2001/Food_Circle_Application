<h2>Hello</h2>

<h1>
  <%= gravatar_for(@user) %>
  <%= @user.username %>
</h1>

<p>
  Current Roles: <%= @user.roles.pluck(:name) %>
</p>

Add Role : 
<%= form_with model: @user do |f| %>
  <%= f.collection_check_boxes :role_ids, Role.all, :id, :name %>
  <%= f.submit class:"btn btn-primary"%>
<% end %>